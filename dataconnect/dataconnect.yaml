specVersion: "v1beta"
serviceId: "sport-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sport-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
